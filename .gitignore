/target
# Workspace member crates (zen-lang, zen-cli, ...) whose test code
# resolves scratch dirs relative to their own crate dir (`cargo test -p`
# sets cwd there), rather than the shared workspace-root /target.
target/
/target_smoke
/dist
bin/
obj/
.zen/runtime.db
.zen/state.json
.zen/schedules.json
.zen/events.log
.zen/build-number
